<% content_for :title, t('events.new') %>

<section class="py-8">
  <div class="container mx-auto max-w-3xl">
    <h1 class="text-2xl font-bold mb-4"><%= t('events.new') %></h1>
    <%= render 'form', event: @event %>
  </div>
</section>