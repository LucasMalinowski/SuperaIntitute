<% content_for :title, t('buttons.edit') %>

<section class="py-8">
  <div class="container mx-auto max-w-3xl">
    <h1 class="text-2xl font-bold mb-4"><%= t('buttons.edit') %> <%= @event.name %></h1>
    <%= render 'form', event: @event %>
  </div>
</section>